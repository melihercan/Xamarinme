<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Title="Hosting"
             x:Class="DemoApp.Views.HostingPage">
    <ContentPage.Content>
        <StackLayout>

            <ListView ItemsSource="{Binding ConfigurationItems}" BackgroundColor="White" HasUnevenRows="true" 
                      SeparatorVisibility="None" VerticalOptions="FillAndExpand">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Spacing="0" BackgroundColor="{Binding BackgroundColor}">
                                <Label Text="{Binding Key}" FontAttributes="Bold" />
                                <Label Text="{Binding Value}" FontSize="Small" VerticalOptions="End" />
                                <!--<Label Text="{Binding Text}" FontSize="Small"/>-->
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>